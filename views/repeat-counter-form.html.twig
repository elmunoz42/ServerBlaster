<!DOCTYPE html>
<html>
    <head>
        <title>Server Blaster</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <link rel="stylesheet" href="css/styles.css">
    </head>
    <body>
      <div class="container">
        <div class="row">
          <div class="col-sm-12">
            <div class="jumbotron" id="title">
              <h1>Server Blaster</h1>
            </div>
            <!-- jumbotron -->
          </div>
          <!-- column title -->
        </div>
        <!-- top row -->
        <div class="row" id="content">
          <div class="col-sm-4">
            <div class="jumbotron">
              <h5>Created by <br> Carlos Munoz Kampff</h5>
              <p>More from my GitHub:</p>
              <ul>
                <li><a href="https://elmunoz42.github.io/CMK-Portfolio/">CMK Portfolio</a></li>
                <li><a href="https://elmunoz42.github.io/trackSuggester2/">trackSuggester2</a></li>
                <a href="https://github.com/elmunoz42?tab=repositories">
                <li>SPMS-website-2.0</li>
                <li>metronomo</li>
                <li>roman-numerals</li>
                <li>leap-year</li>
                <li>Epicodus-Flashcards</li>
                <li>calculator</li>
                <li>Music-Apps</li>
                <li>vacation-spot</li>
                </a>
              </ul>
            </div>
            <!-- jumbotron -->
            <form action="/display" method="post">
              <div class="form-group">
                <label for="word_to_match">word to match (not case sensitive):</label><br>
                <input type="text" name="word_to_match" placeholder="waldo"><br>
                <label for="word_to_replace">word to replace (case sensitive):</label><br>
                <input type="text" name="word_to_replace" placeholder="Waldorf"><br>
                <label for="word_to_search">text to search:</label><br>
                <textarea name="word_to_search" cols="48" rows="10" required="required">
                </textarea><br>
                <input type="reset">
              </div>
              <!-- form group -->
              <button type="submit" class="btn btn-primary" name="button">Submit</button>
            </form>
          </div>
          <!-- left column -->
          <div class="col-sm-8">
            <div class="jumbotron">
              {% if commands is not empty %}
                <ol>
                    {% for command in commands %}
                        <li>$GLOBALS['DB']->query("{{ command }}</li>
                    {% endfor %}
                </ol>
              {% endif %}
              {% if commands2 is not empty %}
                <ol>
                    {% for command in commands2 %}
                        <li>$GLOBALS['DB']->query("{{ command }}</li>
                    {% endfor %}
                </ol>
              {% endif %}
            </div>
            <!-- jumbotron -->
          </div>
          <!-- right column -->
        </div>
        <!-- second row -->
      </div>
      <!-- container -->
    </body>
</html>
